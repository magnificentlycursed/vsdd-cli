# ADOPTED ORACLE (operator adoption 2026-07-22, second batch, recorded
# on vsdd-cli #738; drafted under vsdd-cli #749).
# An OPEN finding with neither owner nor validator is the contract's
# lifecycle falsifier ("a finding without an owner"): the kind files
# and the answer stays phase-4's work — integrity findings report,
# never steer. The owned sibling keeps the in-fixture contrast; the
# discrimination itself is pinned by the paired clean negative,
# integrity-owned-open (vsdd-cli #759).
phase: phase-4
layer: 2
next_action: 4-route-findings
degraded: null
integrity_findings:
  - findings-missing-owner-or-validator
