# DRAFT ORACLE (fix pass, vsdd-cli #749) — awaiting operator adoption.
# A result comment citing a handle the tracker cannot resolve is the
# citation-integrity breach the contract names ("a reference that
# cannot be resolved ... is a defect the conformance checks flag"):
# one unresolvable member files the kind; the resolving sibling shows
# the check keys per handle.
phase: phase-6
layer: 2
next_action: 6-run-convergence-check
degraded: null
integrity_findings:
  - unresolvable-handles-in-result-comments
