# ADOPTED ORACLE (operator adoption 2026-07-22, second batch, recorded
# on vsdd-cli #738; drafted under vsdd-cli #749).
# A result comment citing a handle the tracker cannot resolve is the
# citation-integrity breach the contract names ("a reference that
# cannot be resolved ... is a defect the conformance checks flag"):
# one unresolvable member files the kind; the resolving sibling keeps
# the in-fixture contrast, and the per-handle keying is pinned by the
# paired clean negative, integrity-resolving-handles (vsdd-cli #759).
phase: phase-6
layer: 2
next_action: 6-run-convergence-check
degraded: null
integrity_findings:
  - unresolvable-handles-in-result-comments
