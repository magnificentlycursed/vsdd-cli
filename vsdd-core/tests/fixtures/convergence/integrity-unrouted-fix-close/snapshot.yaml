acquisition_outcome: acquired
milestones:
  - {name: "layer 2: snapshot acquisition, phase answer, and corroboration", state: open, is_active: true}
findings:
  # A fix-close inside the forward-only universe with no filed routing:
  # closed, no disposition (so not an exempt disposition closure), an
  # evidence reference present (so the closed-findings-missing-evidence
  # kind stays silent and this fixture isolates the unrouted kind),
  # closed_before_ratification false (at or after the boundary — in the
  # query's universe), routing_present false. The unrouted-findings kind,
  # and only it.
  - {handle: "the unrouted fix-close", status: closed, owner: quality-engineer, validator: software-engineer, evidence_reference_present: true, disposition: null, routing_present: false, closed_before_ratification: false}
round_manifests: []
round_children: []
comment_handles: []
display_repo_name: vsdd-cli
display_session: "session 4"
display_work_item: "#811 unrouted-findings query"
display_active_milestone: "layer 2: snapshot acquisition, phase answer, and corroboration (0 open)"
