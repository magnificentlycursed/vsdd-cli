# ADOPTED ORACLE (operator adoption 2026-07-22, second batch, recorded
# on vsdd-cli #738; drafted under vsdd-cli #758).
# The prior LAYER's red-gate fail record, carried into this layer's
# 2a: kind, outcome, and phase all match — only the layer differs —
# so the record satisfies no arm and the phase's authoring work
# stands. Dropping the layer conjunct would answer close-phase on a
# red gate this layer never ran: this fixture is that conjunct's
# falsifier.
phase: phase-2a
layer: 2
next_action: 2a-author-red-gate-tests
degraded: null
integrity_findings: []
